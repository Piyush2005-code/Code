unordered_map<int, int> mp;

mp.clear(); //Clears the map elements
mp.size(); //Returns the size of the map
mp.insert({int, int}); //Inserts the key-value pair
mp.erase(key) or mp.erase(mp.begin() + i) //Delete the key-value pair by either key or by iterator, iterator method will contain that element with which it was inserted in the order.
for(auto um: mp){
    cout<< um.first << " " << um.second; // The key and value pairs come out as pair<int, int>
}

mp[1] = 3; // The key and value pairs can be updated or inserted in the given manner.


Two Sum - done through hashmaps (unordered_map)
Product of Array Except Self - done (prefix and suffix product arrays)
Longest Consecutive Sequence - in progress(not complete and having issues in understanding)
Sliding Window Maximum -  
Trapping Rain Water
Largest Rectangle in Histogram
LRU Cache
LFU Cache
Merge k Sorted Lists - Done, but the problem is something you need to remember the little details of the question in order to solve.
Median of Two Sorted Arrays
Kth Largest Element
Find Median from Data Stream
Course Schedule
Number of Islands
Redundant Connection
Accounts Merge
Word Ladder
Minimum Window Substring
Coin Change
Edit Distance
Longest Common Subsequence
Burst Balloons
Network Delay Time
Task Scheduler
Serialize and Deserialize Binary Tree
Trapping Rain Water - Done, but only looked at the solution, Read it again and implement again until you remember it...
Combination Sum - Done, normal but again do it...
Combination Sum II - Not-Done, Do it again.
Valid Sudoku - Done logic-wise, but need to go throuh the efficient solution once again.
Rotate Image - Done, logic is to do vertical rotation and then transpose...
Group Anagrams - Have to do this through the use of efficient methods, as this is not efficient
Pow(x, n) - Have to get the half calculation first and then use it again and again to reduce the complexity from O(n) to O(log(n))
Next Permutation - This has some code which I need to understand carefully because I solved it in a manner that it has only made me memorize the solution, but cannot solve it by my own logic.
Permutations - A Flagship question in my journey of DSA, just because I was able to solve it in one go...May the force be with you...
N-Queens - Done with a No good approach actually applied.
Maximum Subarray - Good Problem, need to revise through it once.
Jump Game - That's a beautiful approach to solve this question, but I need to take a look at this question again.
Merge Intervals - Overall a very good question, but I would want to review the code again.
Insert Interval - I feel it might be the extension of merge Interval, but it is something a little diff.
3Sum - Done, but I NEED to revise
Best Time To Buy and Sell Stock - Kadane's Algorithm, like a maximum subarray problem this is also similar
Plus One - Done...
Unique Paths - Done, but the problem requires critical thinking and implementation, so I need to learn to implement DP properly.
Spiral Matrix - Supposed to be done with ease, but should not underestimate the implementation of this problem as that is great.
Edit Distance - Brilliant Question of DP, but need to look at it after, I need to learn how to create the dp logic good, the question and logic is something I implemented but need to learn how to create the DP logic.
Longest Substring without Repeating Characters - Did the question through correct method, but use char_set.count(s[j]) instead of char_set.find(s[j]) != char_set.end() in the while() loop.
Longest Repeating Character Replacement - Good Question, Logic was correct but I struggle with the implementation of correct logic in string-based questions
Longest Common Subsequence - DP question, whenever we have a complex problem having a lot of combinatorial patterns and checking, if no normal soltion fits then we need to see for complex DP solution.
Max Consecutive Ones III - Sliding window, with a counter of invalid numbers limited to a maximum value...Done after learning the patterns myself.
Subtree of Another Tree - Done, surprisingly I did it, because it's a bit confusing during the implementing the logic part of the question.
Shortest Common Supersequence - Hard Question, DP solution and then also have to extract the string...Didn't do myself.
Set Matrix Zeroes - The question can be done in O(NM) and has been done in my solution and O(1) space, so just see and learn the solution once, it is good.
Search 2D Matrix - Just assume the 2D matrix as a flattened array and apply the index transformation with mid_i = mid / m; and mid_j = mid % m; and all things similar to simple binary search.
Subsets - Generate all the subsets of the array, including the empty set, and return the power set for the given set...Implemented this and completed it with 100% but I was stuck for quite alot of time and that was a problem as then these kinds of recursive dfs questions should be able to be completed fast enough and not actually take this much time...
Word Search - I implemented the same grid search DFS algorithm but don't know why this doesn't go 100%, same unvisited explore four paths strategy.
Subsets II - The subsets problem but with the extension of non-repeating subsets, this problem is excellent in terms of implementation maturity and how to learn to implement recursive functions carefully, I must practice it again and again.
Interleaving Strings - Did it myself.
Validate Binary Search Tree - Validate left and right subtrees with a range of min and max...also need to create another function of isValid(TreeNode* root, long long min, long long max) which takes these two minimum and maximum of the range and propagates them through the left and right subTrees, in order to validate both of them, also we need to use long long as the datatype for the range variable, some test-cases test the limits.
Same Tree - Checking whether the two given trees are structurally same or not...
Binary Tree Level Order Traversal - Completed, through a queue-based system, and having levels of the tree also pushed into the queue as well, so when creating the vector<vector<int>> res, we maintain all the levels as seperate vectors.
Last Stone Weight - Simple priority queue application can be applied, need to remember the syntax for the priority queue...Did it.
Max Depth of a Binary Tree - Recursive function was enough...
Balanced Binary Tree - To have the absolute difference between the heights of left and right subtree to be less than one as well as both the subtrees are also balanced...
Longest Consecutive Sequence - Most Obvious thought would be to sort and then apply sliding window(although cannot call it sliding window with this approach), but optimized in O(n) is make a hashmap; which will give you O(n) once, check each element in the map, such that if num - 1 exists or not, if it does exist... then skip and if it doesn't then that is the start of the sequence, and we can say that if we start from there with checking num + 1 and increase num++; len++; with it, then we will definitely find the maximum length of the consecutive sequence...and particularly, array questions can be done in O(n) with the use of hashmaps.
Surrounded Regions - Now this is a problem which can be problematic if you didn't understand it completely as then you wouldn't be able to get it right...So the problem is that any of the 'O' regions(regions can be spanned through DFS) which are touching the corners, mark them 'S' first and then mark all other regions, which are 'O' to be 'X', as they are now the ones to be surrouned by 'X's, then to turn all 'S' into 'O', then the work would be done...
Clone Graph - clone each and every node without creating multiple duplicates, maintain a hashmap which maintains a map from original to the copy of the node, and we will the return the already created copy of the node only when the node's copy already exists...
Network Delay Time - Apply the dijkstra's algorithm, it's easy.
Kth Largest Element in a Stream - Maintain a minheap(Yes a minHeap) and then only keep the top k in the minheap by keeping the size of the heap to be three in the minheap so as to only contain the k largest into the min heap and the top will then refer to the Kth largest element.




Tasks:
 - Fin out what is Kadane's Algorithm and implement it.
 - Master Kadane's Algorithm.
 - Edit Distance DP  question
 - Do DP questions.

Toolkit:
Syntax for priority_queue<int> pq(vect.begin(), vect.end()), priority_queue<int, vector<int>, compare> where compare is a bool struct{} that returns a value;

Patterns:

 - Sliding Window:
 -- Expand Right

    While Invalid:
        Shrink Left

    Update Answer

 - 


